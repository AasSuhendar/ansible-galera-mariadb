---
- name: Remove Exsisting galera.cnf
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /etc/mysql/conf.d/galera.cnf

- name: Remove DB
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - /var/lib/mysql/
  when: 
    - full_uninstall