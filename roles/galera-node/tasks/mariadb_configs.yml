---
- name: Generate MariaDB configuration galera.cnf
  template:
    src: galera.cnf.j2
    dest: /etc/mysql/conf.d/galera.cnf
    owner: "{{ mariadb_file_owner }}"
    group: "{{ mariadb_file_owner }}"
    mode: 0644

- name: Intiate MariaDB Galera Node Superuser
  shell: sudo galera_new_cluster
  when:
    - primary
